# 安全与隐私

AIPDF 目前是本地开发版本，尚未接受独立安全审计；没有承诺用于处理恶意输入的强隔离或企业级安全认证。

## 使用边界

- 源文件保持只读，输出另存。普通文档处理没有设计上传功能。
- 网址转 PDF 会让 WebKit 获取网址、页面脚本及资源；不是离线功能。Office 文件也可能包含外部资源，不应据“在本机处理”推断没有任何网络风险。
- Python 处理器及本地转换组件不是安全沙箱，通常以当前用户权限运行。不要把未知来源的恶意文件交给持有敏感资料的日常账户处理。
- 大文件、复杂页面及压缩炸弹可能消耗大量内存、CPU 或磁盘；目前没有覆盖全部处理步骤的资源配额。
- 裁剪保留隐藏区域；需要脱敏时用永久遮盖，并复查文字、图片、附件及其他页面。可视签名不等于证书数字签名。
- 工作流程保留中间文件；取消或失败可能留下部分输出。处理记录保存输出路径，清空记录不会删除输出文件。发布 Issue 或截图前自行检查这些内容。
- 输入密码不写入持久记录；没有承诺对内存、交换文件、系统备份或磁盘进行安全擦除。

## 报告问题

公开 Issue 中不要附真实合同、身份证件、密码、访问令牌、带个人路径的日志或可直接利用的漏洞细节。优先提供最小合成样例、系统版本、工具名称和复现步骤。

涉及漏洞时，如果仓库 Security 页面已提供 **Report a vulnerability**，使用该私密入口。若入口尚未启用，只发布不含漏洞细节的联系请求，与维护者约定私下渠道后再传递材料。此文档本身不会启用 GitHub 的私密报告功能，也不承诺响应时限。

如果凭据曾进入 Git 历史，先吊销或轮换，再协调清理历史及其他副本。仅删除当前文件或添加 `.gitignore` 不能移除旧提交中的内容。

发布注意事项见 [分发说明](docs/分发说明.md)。
